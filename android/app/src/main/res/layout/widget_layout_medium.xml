<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <!-- Header: "СЛЕДУЮЩАЯ ПАРА" -->
    <TextView
        android:id="@+id/textHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_header"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/widget_header"
        android:textAllCaps="true"
        android:letterSpacing="0.05" />

    <!-- Subject name -->
    <TextView
        android:id="@+id/textSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/widget_subject"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="2dp" />

    <!-- Lesson type -->
    <TextView
        android:id="@+id/textType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_marginTop="2dp" />

    <!-- Time -->
    <TextView
        android:id="@+id/textTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@color/widget_time"
        android:layout_marginTop="2dp" />

    <!-- Location -->
    <TextView
        android:id="@+id/textLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="@color/widget_location"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginTop="1dp" />

    <!-- Countdown / date (static, for future days) -->
    <TextView
        android:id="@+id/textCountdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:layout_marginTop="2dp" />

    <!-- Real-time countdown (today only, ticks every second) -->
    <Chronometer
        android:id="@+id/chronometerCountdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/widget_countdown_today"
        android:countDown="true"
        android:visibility="gone"
        android:layout_marginTop="2dp" />

</LinearLayout>
