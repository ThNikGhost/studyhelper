# План: SchedulePage для фронтенда

> **Статус**: Отложено до реализации парсера расписания ОмГУ
> **Создан**: 2026-02-04

## Цель
Создать страницу расписания с просмотром на неделю/день, навигацией между неделями и отображением текущей пары.

## Файлы для создания/изменения

### 1. `frontend/src/types/schedule.ts` (новый)
TypeScript типы, соответствующие backend schemas:
- `LessonType` — enum типов занятий
- `DayOfWeek` — enum дней недели
- `WeekType` — enum типа недели (odd/even)
- `ScheduleEntry` — запись расписания
- `DaySchedule` — расписание на день
- `WeekSchedule` — расписание на неделю
- `CurrentLesson` — текущая/следующая пара

### 2. `frontend/src/services/scheduleService.ts` (новый)
API сервис с функциями:
- `getWeekSchedule(targetDate?: string)` — GET /schedule/week
- `getTodaySchedule(targetDate?: string)` — GET /schedule/today
- `getCurrentLesson()` — GET /schedule/current

### 3. `frontend/src/components/schedule/LessonCard.tsx` (новый)
Компонент карточки одной пары:
- Время (start_time - end_time)
- Название предмета (subject_name)
- Тип занятия (lesson_type) с цветовой индикацией
- Преподаватель (teacher_name)
- Аудитория (room, building)
- Примечания (notes)

### 4. `frontend/src/components/schedule/DayScheduleCard.tsx` (новый)
Компонент расписания на день:
- Заголовок с названием дня и датой
- Список LessonCard для каждой пары
- Состояние "нет пар" если entries пустой

### 5. `frontend/src/pages/SchedulePage.tsx` (новый)
Основная страница:
- Header с кнопкой "назад" и заголовком
- Переключатель вида: "Неделя" / "День"
- Навигация: кнопки "←" и "→" для переключения недель
- Индикатор недели (номер, чёт/нечёт)
- Список DayScheduleCard для каждого дня недели
- React Query для запросов к API
- Состояния: loading, error, empty

### 6. `frontend/src/App.tsx` (изменить)
- Добавить импорт SchedulePage
- Заменить PlaceholderPage на SchedulePage для /schedule

## Структура компонентов

```
SchedulePage
├── Header (заголовок + кнопка назад)
├── ViewToggle (Неделя / День)
├── WeekNavigation (← Неделя 6 (чёт) →)
└── WeekSchedule
    ├── DayScheduleCard (Понедельник, 03.02)
    │   ├── LessonCard (09:00 - 10:30 Математика)
    │   └── LessonCard (10:40 - 12:10 Физика)
    ├── DayScheduleCard (Вторник, 04.02)
    │   └── ...
    └── ...
```

## Дизайн LessonCard

```
┌─────────────────────────────────────────┐
│ 09:00 - 10:30           [Лекция]        │
│ Математический анализ                    │
│ 👤 Иванов И.И.     📍 ауд. 205, Главное │
└─────────────────────────────────────────┘
```

Цвета типов занятий:
- lecture — blue
- practice — green
- lab — orange
- seminar — purple
- exam — red
- consultation — gray

## API endpoints

| Метод | Endpoint | Параметры | Описание |
|-------|----------|-----------|----------|
| GET | /schedule/week | target_date? | Расписание на неделю |
| GET | /schedule/today | target_date? | Расписание на день |
| GET | /schedule/current | — | Текущая/следующая пара |

## React Query hooks

```typescript
// Расписание на неделю
useQuery({
  queryKey: ['schedule', 'week', targetDate],
  queryFn: () => scheduleService.getWeekSchedule(targetDate),
})

// Текущая пара (обновление каждую минуту)
useQuery({
  queryKey: ['schedule', 'current'],
  queryFn: () => scheduleService.getCurrentLesson(),
  refetchInterval: 60000,
})
```

## Порядок реализации

1. [ ] types/schedule.ts — типы данных
2. [ ] services/scheduleService.ts — API сервис
3. [ ] components/schedule/LessonCard.tsx — карточка пары
4. [ ] components/schedule/DayScheduleCard.tsx — день
5. [ ] pages/SchedulePage.tsx — страница
6. [ ] App.tsx — подключение роута

## Верификация

1. `npm run dev` — запустить frontend
2. `cd backend && uv run uvicorn src.main:app --reload` — запустить backend
3. Войти в приложение
4. Перейти на /schedule
5. Проверить:
   - Загрузка расписания на неделю
   - Переключение недель (← →)
   - Переключение вид Неделя/День
   - Отображение пар
   - Состояние "нет данных" если расписание пустое
6. `npm run lint` — проверка линтера
7. `npm run build` — проверка сборки
