# Задача: Виджет "Ближайшее" на Dashboard

## Приоритет: P1 (высокий)
## Сложность: Низкая
## Затрагивает: Frontend

## Описание
Улучшить DashboardPage: объединить информацию о следующей паре и ближайших дедлайнах в компактный, информативный виджет. Добавить таймеры, цветовую индикацию, быстрые действия.

## Зачем
Dashboard — первое, что видит студент. Чем быстрее он понимает "что дальше" — тем полезнее приложение.

---

## Чеклист

### Фаза 1: Рефакторинг DashboardPage
- [ ] Извлечь текущие виджеты в отдельные компоненты:
  - `components/dashboard/NextLessonWidget.tsx`
  - `components/dashboard/DeadlinesWidget.tsx`
  - `components/dashboard/QuickActionsWidget.tsx`
- [ ] Создать `components/dashboard/TodaySummaryWidget.tsx`:
  - Сколько пар сегодня, первая/последняя пара
  - Сколько дедлайнов на этой неделе
  - Есть ли просроченные работы (красный индикатор)

### Фаза 2: Улучшение NextLessonWidget
- [ ] Живой таймер обратного отсчёта до конца текущей пары
- [ ] Показать аудиторию + корпус
- [ ] Показать тип пары (лекция/практика/лабораторная) с цветным бейджем
- [ ] "Следующая пара через X мин" или "Пар больше нет" или "Сегодня выходной"
- [ ] Weekday-aware: в выходные показать расписание на понедельник

### Фаза 3: Улучшение DeadlinesWidget
- [ ] Цветовая индикация: красный (просрочено), оранжевый (сегодня-завтра), жёлтый (эта неделя), зелёный (далеко)
- [ ] Группировка: "Просрочено (2)", "Сегодня (1)", "Эта неделя (3)"
- [ ] Quick action: отметить как "сдано" прямо с dashboard
- [ ] Показать прогресс: "Сдано 5 из 12 работ"
- [ ] Пустое состояние: "Нет ближайших дедлайнов 🎉"

### Фаза 4: Адаптивная сетка
- [ ] Mobile: один столбец, виджеты стекаются
- [ ] Tablet: два столбца
- [ ] Desktop: два столбца с равной шириной
- [ ] Порядок на mobile: сводка → текущая пара → дедлайны → действия

### Фаза 5: Тесты
- [ ] Тесты для TodaySummaryWidget
- [ ] Тесты для обновлённого NextLessonWidget
- [ ] Обновить существующие тесты DashboardPage

---

## Технические детали

### TodaySummaryWidget
```tsx
// Компактная строка-сводка
<div className="flex items-center gap-4 p-4 bg-blue-50 rounded-lg">
  <div>📅 Сегодня: 3 пары (8:30 — 15:00)</div>
  <div>⚡ Дедлайнов на неделе: 2</div>
  {overdue > 0 && <div className="text-red-600">⚠ Просрочено: {overdue}</div>}
</div>
```

### Живой таймер
```tsx
// Обновление каждую минуту
useEffect(() => {
  const interval = setInterval(() => {
    setTimeLeft(calculateTimeLeft(lesson.end_time))
  }, 60_000)
  return () => clearInterval(interval)
}, [lesson])
```

### Quick action — изменить статус работы
```tsx
// Кнопка "Сдано" на карточке дедлайна
const markAsSubmitted = useMutation({
  mutationFn: (workId: number) => workService.updateStatus(workId, 'submitted'),
  onSuccess: () => queryClient.invalidateQueries({ queryKey: ['upcoming-works'] }),
})
```

## Связанные файлы
- `frontend/src/pages/DashboardPage.tsx` — основной файл
- `frontend/src/services/scheduleService.ts` — данные о расписании
- `frontend/src/services/workService.ts` — данные о работах
- `frontend/src/types/schedule.ts`, `frontend/src/types/work.ts`
